apiVersion: v1
kind: Service
metadata:
  name: calc-demo-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: calc-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080